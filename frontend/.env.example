# URL of your deployed FastAPI backend, including the /api prefix if your
# backend mounts routes there. Leave unset for local dev (Vite proxies
# '/api' to your local uvicorn server instead — see vite.config.ts).
#
# Example: https://aca-backend.onrender.com/api
VITE_API_URL=
